<!DOCTYPE html>
<html>
<head>
    <!--<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">-->
	 <link  type="text/css" rel="stylesheet" href="/bootcss/bootstrap.min.css"/>
	  <script type="text/javascript" src="/jquery/jquery.min.js" crossorigin="anonymous"></script>
   
    <!-- BUTTON CLICKS NEED tHIS -->
    <script src="/asset/plugins/jquery/jquery.min.js"></script>
	<!-- jQuery UI 1.11.4 -->
     <script src="/asset/plugins/jquery-ui/jquery-ui.min.js"></script>	
     <script  src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>	
   <!-- <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script> -->
   <!-- <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script> -->
	  <script type="text/javascript" src="/js/bootstrap.min.js"  crossorigin="anonymous"></script>

   <!-- <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>-->
    <link rel="stylesheet" type="text/css" href="/asset/css/uploadaddhar.css">
 <script src="https://cdn.jsdelivr.net/npm/dynamsoft-javascript-barcode@8.2.5/dist/dbr.js"
      data-productKeys="t0068NQAAACwiu6aXm/RiDD7y6oAZlmMZOA5c6n1Gz8g3RyCV8qM7FcX1Yv8iY1GRN+ZerYU/hXcIeg2CIfAJzhsms30pQ40="></script>
   <!-- https://jsfiddle.net/bootstrapious/8w7a50n2 -->

   <link type="text/css" rel="Stylesheet" href="/asset/css/style_new.css" />
     <script type="text/javascript">
      $(document).ready(function () {
     
        console.log("UPSTOX LOGGED IN " )
           // PROCEED ACCESS BUTTNO 
      $('#proceedAccessBtn').click(function(){
        var selText = $('#auth_code').text().trim();
        var codeText = $('#code').text().trim();
        console.log("... UPSTOX ACCESS QUOTED initated "  );
        var   parameters = [] ;
        parameters["auth_code"] =`${selText}`;
        let finalCode= '';
        var query ='';
        if(selText !==null && selText !==undefined &&    selText !==''){
            query ="auth_code="+selText;
            finalCode=selText;
        }
        else if(codeText !==null && codeText !==undefined && codeText !==''){
             query ="auth_code="+codeText;
              finalCode=codeText;
        } 

        if(finalCode !== undefined  && finalCode !== null && finalCode !== '' ){ 
           localStorage.setItem("upstoxAuthCode",finalCode);
         }
       /* if(hidenExtendedToken !== undefined && hidenExtendedToken !== null && hidenExtendedToken !== ''){ 
          localStorage.setItem("upstoxExtendedToken",hidenExtendedToken);
         } 
          if(hidenUserId !== undefined  && hidenUserId !== null && hidenUserId !== '' ){ 
             localStorage.setItem("upstoxUserId",hidenUserId);
             } 
          if(hidenUserName !== undefined  && hidenUserName !== null && hidenUserName !== '' ){      
             localStorage.setItem("upstoxUserName",hidenUserName);
          } 
             */
        //window.location.assign( "https://localhost:56322/fyersgetaccess?"+query);
         // this is for later taking the access_token 
        // window.location.assign( "https://192.168.1.6:8888/.netlify/functions/netlifyupstoxbridge/api/upstoxgetaccess?"+query);
         window.location.assign( "https://192.168.1.6:8888/.netlify/functions/netlifyupstoxbridge/api/upstoxauthcodeverify?"+query);
       //  the above will show the final authenticated Button page i.e. upstox_show_logged_in.ejs 
        // the upstox_show_logged_in.ejs  also does the same state matching --- in case test the websocket as 
        // with fyers from the localhost 9341 etc ... 
        // else the purpose of the upstox_show_logged_in.ejs is to set the 
         // localStorage.setItem("upstoxToken",JSON.stringify(token));
         // window.location.assign( "https://store-stocks.netlify.app/.netlify/functions/netlifystockfyersbridge/api/fyersgetaccess?"+query);
            // DO GET
        /*$.ajax({
            type : "GET",
            url : "https://localhost:56322/",
            contentType : "application/json",
            // data : {platformAjax:platform, genreAjax:genre},
            data : parameters,
            dataType : "json",
            
        success: async function(data, textStatus, jqXHR) {
            if(data.Success)		
                    { console.log("moeny flow records " +JSON.stringify(data.Success.length)); }
            try {
                var dummyVar =  JSON.stringify(data)  ;
                $("#userdata > tbody").html("");
                refreshOPENINTEREST(dummyVar);
                } catch(error) {
                    console.log("moeny flow Data unable to PARSE " +JSON.stringify(error));
                }
            }
            })
            */
    	});




     });
    
	</script>
      <style type="text/css">
        .borderbottom
                {
                        border-bottom:1px solid #dbdbdb;
            }
            @media (min-width: 1200px) {
        .container {
            max-width: 100%;
        }
        .input-sm{ font-size:.8rem; }
    }
    </style>
</head>

<body>
   <div class="container py-5">
                <h1> UPSTOX LOGGED IN </h1>
                    <!--    PUSHLIEDDAYUPSTOX_S:s,
            PUSHLIEDDAYUPSTOX_CODE : code,
            PUSHLIEDDAYUPSTOX_AUTH_CODE: auth_code,-->
                <div> 
                        <p>     <%=PUSHLIEDDAYUPSTOX_S%></p>
                        <p id="code" style="display:none;">     <%=PUSHLIEDDAYUPSTOX_CODE%></p>
                        <p id="auth_code" style="display:none;">     <%=PUSHLIEDDAYUPSTOX_AUTH_CODE%></p>

                        <button  id="proceedAccessBtn" type="button" class="btn btn-primary btn-block mt-4">Proceed Access</button>
                </div>
    </div>
 </form>
</div>

	<!-- 
		for (var i=0; (i+1+chunk) < lenghSt ; i+=chunk) {	array2 += array1.substring(i, i+chunk) + " \n"; console.log("array2 "+array2);  }
	-->


</body>
</html>
         